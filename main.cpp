#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include "functions.hpp"
int cond = 0;
int main() {
    while (cond == 0) {
    menu();
    if(condition == 1) {
        return 0;
    }
    book_list();
    }
    return 0;
}

